# This module contains GraduationRequirements functions.

def deleteArea(AreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/Area/" + AreaID, verb = "delete")

def deleteCareerPlanDeclarationTimePeriod(CareerPlanDeclarationTimePeriodID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CareerPlanDeclarationTimePeriod/" + CareerPlanDeclarationTimePeriodID, verb = "delete")

def deleteCareerPlanDeclarationTimePeriodGradeReference(CareerPlanDeclarationTimePeriodGradeReferenceID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CareerPlanDeclarationTimePeriodGradeReference/" + CareerPlanDeclarationTimePeriodGradeReferenceID, verb = "delete")

def deleteCareerPlanDeclarationTimePeriodStudentEntityYear(CareerPlanDeclarationTimePeriodStudentEntityYearID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CareerPlanDeclarationTimePeriodStudentEntityYear/" + CareerPlanDeclarationTimePeriodStudentEntityYearID, verb = "delete")

def deleteCareerPlanGradeLevel(CareerPlanGradeLevelID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CareerPlanGradeLevel/" + CareerPlanGradeLevelID, verb = "delete")

def deleteConfigDistrict(ConfigDistrictID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/ConfigDistrict/" + ConfigDistrictID, verb = "delete")

def deleteCurriculumCluster(CurriculumClusterID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CurriculumCluster/" + CurriculumClusterID, verb = "delete")

def deleteCurriculumClusterCurriculum(CurriculumClusterCurriculumID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CurriculumClusterCurriculum/" + CurriculumClusterCurriculumID, verb = "delete")

def deleteCurriculumClusterDefault(CurriculumClusterDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CurriculumClusterDefault/" + CurriculumClusterDefaultID, verb = "delete")

def deleteCurriculumSubArea(CurriculumSubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/CurriculumSubArea/" + CurriculumSubAreaID, verb = "delete")

def deleteEndorsement(EndorsementID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/Endorsement/" + EndorsementID, verb = "delete")

def deleteEndorsementDeclarationTimePeriod(EndorsementDeclarationTimePeriodID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementDeclarationTimePeriod/" + EndorsementDeclarationTimePeriodID, verb = "delete")

def deleteEndorsementDeclarationTimePeriodGradeReference(EndorsementDeclarationTimePeriodGradeReferenceID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementDeclarationTimePeriodGradeReference/" + EndorsementDeclarationTimePeriodGradeReferenceID, verb = "delete")

def deleteEndorsementDeclarationTimePeriodStudentEntityYear(EndorsementDeclarationTimePeriodStudentEntityYearID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementDeclarationTimePeriodStudentEntityYear/" + EndorsementDeclarationTimePeriodStudentEntityYearID, verb = "delete")

def deleteEndorsementDefault(EndorsementDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementDefault/" + EndorsementDefaultID, verb = "delete")

def deleteEndorsementOption(EndorsementOptionID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementOption/" + EndorsementOptionID, verb = "delete")

def deleteEndorsementOptionDefault(EndorsementOptionDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementOptionDefault/" + EndorsementOptionDefaultID, verb = "delete")

def deleteEndorsementRequirement(EndorsementRequirementID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirement/" + EndorsementRequirementID, verb = "delete")

def deleteEndorsementRequirementAssessment(EndorsementRequirementAssessmentID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessment/" + EndorsementRequirementAssessmentID, verb = "delete")

def deleteEndorsementRequirementAssessmentCluster(EndorsementRequirementAssessmentClusterID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessmentCluster/" + EndorsementRequirementAssessmentClusterID, verb = "delete")

def deleteEndorsementRequirementAssessmentClusterDefault(EndorsementRequirementAssessmentClusterDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessmentClusterDefault/" + EndorsementRequirementAssessmentClusterDefaultID, verb = "delete")

def deleteEndorsementRequirementAssessmentDefault(EndorsementRequirementAssessmentDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessmentDefault/" + EndorsementRequirementAssessmentDefaultID, verb = "delete")

def deleteEndorsementRequirementAssessmentScore(EndorsementRequirementAssessmentScoreID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessmentScore/" + EndorsementRequirementAssessmentScoreID, verb = "delete")

def deleteEndorsementRequirementAssessmentScoreDefault(EndorsementRequirementAssessmentScoreDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementAssessmentScoreDefault/" + EndorsementRequirementAssessmentScoreDefaultID, verb = "delete")

def deleteEndorsementRequirementCurriculum(EndorsementRequirementCurriculumID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementCurriculum/" + EndorsementRequirementCurriculumID, verb = "delete")

def deleteEndorsementRequirementCurriculumDefault(EndorsementRequirementCurriculumDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementCurriculumDefault/" + EndorsementRequirementCurriculumDefaultID, verb = "delete")

def deleteEndorsementRequirementDefault(EndorsementRequirementDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/EndorsementRequirementDefault/" + EndorsementRequirementDefaultID, verb = "delete")

def deletePlan(PlanID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/Plan/" + PlanID, verb = "delete")

def deletePlanDefault(PlanDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/PlanDefault/" + PlanDefaultID, verb = "delete")

def deletePlanEntity(PlanEntityID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/PlanEntity/" + PlanEntityID, verb = "delete")

def deleteQueuedGraduationPlanRecalcTrigger(QueuedGraduationPlanRecalcTriggerID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/QueuedGraduationPlanRecalcTrigger/" + QueuedGraduationPlanRecalcTriggerID, verb = "delete")

def deleteQueuedStudentPlanCourseworkApplication(QueuedStudentPlanCourseworkApplicationID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/QueuedStudentPlanCourseworkApplication/" + QueuedStudentPlanCourseworkApplicationID, verb = "delete")

def deleteStudentArea(StudentAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentArea/" + StudentAreaID, verb = "delete")

def deleteStudentCareerPlan(StudentCareerPlanID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentCareerPlan/" + StudentCareerPlanID, verb = "delete")

def deleteStudentEndorsement(StudentEndorsementID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsement/" + StudentEndorsementID, verb = "delete")

def deleteStudentEndorsementOption(StudentEndorsementOptionID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementOption/" + StudentEndorsementOptionID, verb = "delete")

def deleteStudentEndorsementRequirement(StudentEndorsementRequirementID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementRequirement/" + StudentEndorsementRequirementID, verb = "delete")

def deleteStudentEndorsementRequirementAssessment(StudentEndorsementRequirementAssessmentID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementRequirementAssessment/" + StudentEndorsementRequirementAssessmentID, verb = "delete")

def deleteStudentEndorsementRequirementAssessmentScore(StudentEndorsementRequirementAssessmentScoreID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementRequirementAssessmentScore/" + StudentEndorsementRequirementAssessmentScoreID, verb = "delete")

def deleteStudentEndorsementRequirementCourseRequest(StudentEndorsementRequirementCourseRequestID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementRequirementCourseRequest/" + StudentEndorsementRequirementCourseRequestID, verb = "delete")

def deleteStudentEndorsementRequirementCurriculum(StudentEndorsementRequirementCurriculumID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentEndorsementRequirementCurriculum/" + StudentEndorsementRequirementCurriculumID, verb = "delete")

def deleteStudentPlan(StudentPlanID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentPlan/" + StudentPlanID, verb = "delete")

def deleteStudentPlanThreadLock(StudentPlanThreadLockID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentPlanThreadLock/" + StudentPlanThreadLockID, verb = "delete")

def deleteStudentSubArea(StudentSubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentSubArea/" + StudentSubAreaID, verb = "delete")

def deleteStudentSubAreaCurriculumSubArea(StudentSubAreaCurriculumSubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentSubAreaCurriculumSubArea/" + StudentSubAreaCurriculumSubAreaID, verb = "delete")

def deleteStudentSubAreaWaiver(StudentSubAreaWaiverID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/StudentSubAreaWaiver/" + StudentSubAreaWaiverID, verb = "delete")

def deleteSubArea(SubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/SubArea/" + SubAreaID, verb = "delete")

def deleteSubAreaLimitGroup(SubAreaLimitGroupID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/SubAreaLimitGroup/" + SubAreaLimitGroupID, verb = "delete")

def deleteSubAreaLimitGroupCurriculumSubArea(SubAreaLimitGroupCurriculumSubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/SubAreaLimitGroupCurriculumSubArea/" + SubAreaLimitGroupCurriculumSubAreaID, verb = "delete")

def deleteTempEndorsementDefault(TempEndorsementDefaultID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/TempEndorsementDefault/" + TempEndorsementDefaultID, verb = "delete")

def deleteTempEndorsementImportError(TempEndorsementImportErrorID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/TempEndorsementImportError/" + TempEndorsementImportErrorID, verb = "delete")

def deleteTempFailedStudentSubAreaCurriculumSubArea(TempFailedStudentSubAreaCurriculumSubAreaID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/TempFailedStudentSubAreaCurriculumSubArea/" + TempFailedStudentSubAreaCurriculumSubAreaID, verb = "delete")

def deleteTempFailedStudentSubAreaWaiver(TempFailedStudentSubAreaWaiverID, entity_id = 1):

	make_request(endpoint = "/Generic/" + entity_id + "/GraduationRequirements/TempFailedStudentSubAreaWaiver/" + TempFailedStudentSubAreaWaiverID, verb = "delete")